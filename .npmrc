# Companion postinstall must run so `pi install` of this toolkit can
# register ponytail / pi-lens as independent Pi packages.
# Do not set ignore-scripts=true.
#
# pi-lens native binaries may still fail on Termux; that install is
# non-blocking and is no longer part of this package's npm dependencies.
